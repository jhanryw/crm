# APP
# NEXT_PUBLIC_APP_URL is the domain where the front-end will be hosted via the reverse proxy
NEXT_PUBLIC_APP_URL=https://meu-crm.meudominio.com

# SUPABASE 
# If running in the same Docker network, these might use the internal Docker hostname or the external API Gateway. 
# Depending on how the frontend calls Supabase (if via Server Components it can be internal; if via Client Components it MUST be external).
# Usually NEXT_PUBLIC variables are exposed to the client, so they need to be public accessible URLs.
NEXT_PUBLIC_SUPABASE_URL=https://crm-supabase.meudominio.com
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-production-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-production-service-role-key

# LOGTO
# IMPORTANTE: No Logto auto-hospedado (Self-hosted), o endpoint DEVE terminar com /oidc
LOGTO_ENDPOINT=https://crm-logto.meudominio.com/oidc
LOGTO_APP_ID=your-production-app-id
LOGTO_APP_SECRET=your-production-app-secret
# These redirect URIs are your public app domains
LOGTO_REDIRECT_URI=https://meu-crm.meudominio.com/api/auth/callback/logto
LOGTO_POST_LOGOUT_REDIRECT_URI=https://meu-crm.meudominio.com/

# FEATURES
FEATURE_META_INBOX=false
FEATURE_WHATSAPP_OFFICIAL=false
FEATURE_EMAIL_MARKETING=false
FEATURE_CONVERSIONS=false

# WEBHOOKS / TRACKING
WA_TRACKING_REGEX=(CAMP-[0-9A-Z]{6,})
WEBHOOK_SIGNING_SECRET=your-production-webhook-secret
